// examples/demo.ltl — runnable demo for ltl_svg_draw

import std::io
use ltl_svg_draw as lib

fn main() {
    io::println("ltl_svg_draw demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}